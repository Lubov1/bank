apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-dashboards
data:
{{- range $path, $file := .Files.Glob "config/*.json" }}
  {{ base $path }}: |
{{ $file | toString | nindent 4 }}
{{- end }}