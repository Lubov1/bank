spring:
  application:
    name: exchange-generator
  config:
    import: ""
  cloud:
    consul:
      enabled: false
  discovery:
    enabled: false

exchange:
  prefix: exchange
gateway:
  prefix: gateway